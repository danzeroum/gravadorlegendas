# Dependências para captura e transcrição de áudio
# Instalar com: pip install -r requirements/audio.txt
# ou: pip install .[audio]

# Captura WASAPI loopback (Windows)
PyAudio>=0.2.14

# Transcrição local (faster-whisper com CTranslate2)
faster-whisper>=1.0.0

# VAD (Voice Activity Detection)
silero-vad>=4.0.0

# Processamento de áudio
SoundFile>=0.12.0
numpy>=1.24.0

# --- Frente C (curto prazo): RNNoise — supressão de ruído em tempo real ---
# O filtro funciona com fallback espectral se nenhum binding estiver instalado,
# mas a qualidade é significativamente melhor com RNNoise nativo.
# Descomente um dos bindings abaixo para usar RNNoise real:
# pyrnnoise>=0.0.1
# rnnoise-wrapper>=0.0.1

# --- Diarização (opcional) ---
# Instalar separadamente com: pip install diart pyannote.audio
# Requer token Hugging Face: https://hf.co/settings/tokens
# diart>=0.7.0
# pyannote.audio>=3.1.0
